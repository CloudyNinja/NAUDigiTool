const countElementsInEquationArray = require('./countElementsInEquationArray');

test('Test will count elements in the equation array', () =>
{
    expect( countElementsInEquationArray() ).toBe( 3 );
});